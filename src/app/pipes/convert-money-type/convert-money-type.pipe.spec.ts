import { ConvertMoneyTypePipe } from './convert-money-type.pipe';

describe('ConvertMoneyTypePipe', () => {
  it('create an instance', () => {
    const pipe = new ConvertMoneyTypePipe();
    expect(pipe).toBeTruthy();
  });
});
