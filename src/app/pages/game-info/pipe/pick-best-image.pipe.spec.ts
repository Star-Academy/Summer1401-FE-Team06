import { PickBestImagePipe } from './pick-best-image.pipe';

describe('PickBestImagePipe', () => {
  it('create an instance', () => {
    const pipe = new PickBestImagePipe();
    expect(pipe).toBeTruthy();
  });
});
