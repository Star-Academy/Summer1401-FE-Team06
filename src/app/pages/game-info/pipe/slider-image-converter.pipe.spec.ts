import { SliderImageConverterPipe } from './slider-image-converter.pipe';

describe('SliderImageConverterPipe', () => {
  it('create an instance', () => {
    const pipe = new SliderImageConverterPipe();
    expect(pipe).toBeTruthy();
  });
});
